services:
  - type: web
    name: ipa-xerez-landing
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    routes:
      - type: rewrite
        source: "/*"
        destination: "/index.html"
